# Настройки проекта (обязательно к промпту монтажа)

## Субтитры — ВЫКЛЮЧЕНЫ

```json
"subtitles": false
```

В `edit/data.json` (паспорт проекта).

**Агент:**
- не создаёт `subtitles.json`;
- не добавляет субтитры / captions в HyperFrames-композицию;
- не рендерит текст внизу кадра.

**Whisper / транскрипция** — только для монтажа (EDL, тайминги графики, проверка речи), не для вывода на экран.

При вставке `MONTAGE-PROMPT-v4.md` в чат **добавляйте этот файл** или строку:

> Субтитры отключены (`subtitles: false`). Раздел 5 промпта не выполнять.
